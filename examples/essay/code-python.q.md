## [essay-code-python] Fibonacci

    type: essay
    input: code
    language: python

Write a Python function `fib(n)` that returns the n-th Fibonacci number
(with `fib(0) == 0` and `fib(1) == 1`). You may use either an iterative
or a recursive approach.
