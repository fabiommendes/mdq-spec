Which snippet correctly computes the nth Fibonacci number recursively?

* [*] ```python
  def fib(n):
      if n < 2:
          return n
      return fib(n - 1) + fib(n - 2)
  ```
* [ ] ```python
  def fib(n):
      return fib(n - 1) + fib(n - 2)
  ```
