# NOT_YET_SUPPORTED (multiple-choice.fenced-code-choice.mdq.md): the parser
# folds every choice's body into single-line prose the same way it folds
# preamble/stem text, so a multi-line fenced code block nested in a choice
# collapses into one line and stops being valid fenced-code Markdown. This
# file documents the *correct* document -- the raw code block preserved
# verbatim, the way a fenced block in a preamble already is -- not what the
# parser currently produces. See BACKLOG.md.
type: multiple-choice
stem: Which snippet correctly computes the nth Fibonacci number recursively?
choices:
  - text: |-
      ```python
      def fib(n):
          if n < 2:
              return n
          return fib(n - 1) + fib(n - 2)
      ```
    score: 1
  - text: |-
      ```python
      def fib(n):
          return fib(n - 1) + fib(n - 2)
      ```
    score: 0
