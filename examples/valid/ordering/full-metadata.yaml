# Every generic frontmatter field plus every field ordering adds, all
# declared explicitly. `content` and `highlight` would have been inferred
# from the fenced code block anyway; a declared value simply wins over what
# the body implies.
#
# `normalizations` is spelled as a list here, the other half of the
# shorthand `skip-blanks.mdq.md` shows.
#
# The rejected alternative differs from the answer key by a single line,
# and the distractor it uses is declared in `## [extra]` so the student can
# actually build it. Its observations carry the feedback first and the
# author-facing `!` comment after -- either order is allowed, as long as
# the two blocks do not interleave.
id: ordering-full-metadata
uuid: 3b1f0c52-9d7a-4f16-8b2e-1c0a7d6e4f93
title: Decaimento radioativo
author: Fábio Macêdo Mendes
locale: pt-BR
type: ordering
stem: >-
  Ordene as linhas abaixo em um programa que simula o decaimento radioativo de
  uma amostra, imprimindo quantos núcleos restam a cada passo de tempo.
content: code
highlight: python
indentation: strict
unmatched: incorrect
normalizations:
  - skip-blanks
lines:
  - [0, "from math import exp"]
  - [0, ""]
  - [0, "n0, meia_vida = 1000, 5.0"]
  - [0, ""]
  - [0, "for t in range(11):"]
  - [1, "n = n0 * exp(-0.693 * t / meia_vida)"]
  - [1, "print(t, round(n))"]
extra:
  - [1, "n = n0 * exp(0.693 * t / meia_vida)"]
reject:
  - lines:
      - [0, "from math import exp"]
      - [0, ""]
      - [0, "n0, meia_vida = 1000, 5.0"]
      - [0, ""]
      - [0, "for t in range(11):"]
      - [1, "n = n0 * exp(0.693 * t / meia_vida)"]
      - [1, "print(t, round(n))"]
    feedback: >-
      O expoente do decaimento é negativo: com `exp(+0.693 * t / meia_vida)` a
      amostra cresceria em vez de decair.
    comment: Erro de sinal, o mais comum nesta questão.
tags:
  - fisica
  - python
meta:
  difficulty: hard
