<!-- Describe the change and link the issue it resolves. -->

- [ ] The specification (`docs/`, `schema/`) is updated, if the format changes.
- [ ] New or changed behavior has examples in `examples/`.
- [ ] `uv run scripts/schema_bundle.py --check` passes.
- [ ] The tests of the changed implementations pass.
